name: Draft release notes

on:
  push:
    tags:
      - '*'
    branches:
      - main

# Both triggers update the same draft release, so runs must not overlap. The
# group is deliberately ref-independent: a main push and a tag push racing each
# other is exactly the case being serialised.
concurrency:
  group: draft-release-notes
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  release-drafter:
    name: Draft release notes
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pull-requests: read # Read PR titles for the changelog; no autolabeler.

    steps:
      - name: Set version
        id: version
        env:
          RELEASE_VERSION_SCHEME: ${{ vars.RELEASE_VERSION_SCHEME }}
        run: |
          SCHEME="${RELEASE_VERSION_SCHEME}"
          if [ "${SCHEME}" = "calver" ]; then
            VERSION="$(date "+%y.%-m").0"
            echo "version=${VERSION}" >> "${GITHUB_OUTPUT}"
            echo "Using CalVer: ${VERSION}"
          else
            echo "Using SemVer (default)"
          fi

      - name: Draft release notes
        uses: release-drafter/release-drafter@34d80673e067bdc0c24568d3af899c216adcfaa9 # v7
        with:
          tag: ${{ steps.version.outputs.version }}
          name: ${{ steps.version.outputs.version }}
          version: ${{ steps.version.outputs.version }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
