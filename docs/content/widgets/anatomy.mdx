---
title: Anatomy
description: 'The parts of a widget, named: the panel row, the open editor, and every line each of them draws.'
keywords: ['anatomy', 'nomenclature', 'parts', 'widgets', 'tui']
---

# Anatomy of a widget

Every field draws itself twice. In the panel it is a **row**: one line carrying the answer, with any supporting lines beneath it. Open the row and it becomes an **editor**: the widget takes over the space to the right of the label and draws whatever it needs to collect the answer.

This page names each part. The names are the vocabulary the rest of the documentation and the source use, so a bug report can say "the constraint line is wrong" and mean exactly one thing.

:::note

The frames are real captured output, with a marker added beside each part. The vocabulary is the agreed target: the [mapping](#what-the-code-calls-these-today) shows where the source still uses a different word.

:::

## The row

A field that is not open draws one value line plus whatever supporting lines it declares.

```
╭──────────────────────────────────────────────────────────────────────────╮
│ Orchard order › Basket①                                                  │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│ ❯② Basket③  apple④                                                       │
│     Pick the produce for this delivery. ⑤                                │
│     Space toggles, Enter confirms. ⑥                                     │
│                                                                          │
│   ▼⑦                                                                     │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│ ↑/↓ move · ↵ select · esc back · q quit · ? help ⑧                       │
╰──────────────────────────────────────────────────────────────────────────╯
```

① **breadcrumb** - the trail of panel titles.
② **marker** - which row has focus.
③ **label** - the field's name.
④ **value** - the settled answer.
⑤ **description** - the field's explanatory text.
⑥ **instruction** - the field's short how-to line.
⑦ **overflow** - content continues above or below.
⑧ **key legend** - the keys available right now.

## The editor

Opening the row hands the region right of the label to the widget. A multiple-choice field with bounds and option detail draws the fullest set of parts.

```
╭──────────────────────────────────────────────────────────────────────────╮
│ Orchard order › Basket                                                   │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│ ❯ Basket    ◼⑨ Apple⑩                                                    │
│           ❯⑪ ◼ Carrot                                                    │
│             ◻ Tomato (out of season)⑫                                    │
│           Select between 2 and 3 items. ⑬                                │
│           Stays crisp for weeks when kept cold. ⑭                        │
│     Pick the produce for this delivery. ⑤                                │
│   ▼⑦                                                                     │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│ ↑/↓ move · ↵ accept · esc cancel ⑧                                       │
╰──────────────────────────────────────────────────────────────────────────╯
```

⑨ **selector** - the per-entry chosen-state glyph.
⑩ **entry** - one line of the list the widget offers.
⑪ **entry marker** - which entry is highlighted.
⑫ **entry note** - a qualifier, such as why an entry is unavailable.
⑬ **constraint** - what the field expects, shown before anything is rejected.
⑭ **entry detail** - the highlighted entry's own explanatory text.

## The file picker

The same skeleton over a directory listing, plus the one part unique to it.

```
╭──────────────────────────────────────────────────────────────────────────╮
│ Orchard order › Price list                                               │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│ ❯ Price list  sample-project⑮                                            │
│                 baskets/⑩                                                │
│               ❯⑪ deliveries/                                             │
│                 harvest.csv                                              │
│               Files only. Extensions: csv. Max 2 MB. ⑬                   │
│     The CSV the orchard sends each week. ⑤                               │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│ ↑/↓ move · → open · ← up · ↵ select · tab hidden · esc cancel ⑧          │
╰──────────────────────────────────────────────────────────────────────────╯
```

⑮ **location** - the directory currently being browsed.

## A text editor

No list, so a buffer and a caret take the place of entries.

```
╭──────────────────────────────────────────────────────────────────────────╮
│ Orchard order › Crate                                                    │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│ ❯ Crate label  valley-pear█⑰-a⑯                                          │
│                filling in Fruit ⑱                                        │
│     Identifies the crate on the loading dock. ⑤                          │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│ ↓/↑ next/previous · ↵ accept · esc cancel ⑧                              │
╰──────────────────────────────────────────────────────────────────────────╯
```

⑯ **buffer** - the in-progress text, before it is accepted.
⑰ **caret** - the insertion point within the buffer.
⑱ **activity** - what the widget is doing right now.

## One line, two states

The constraint ⑬ and the error ⑲ are the same physical line. The constraint is guidance shown *before* anything goes wrong; the moment a value is rejected the error replaces it, so the two never stack. Compare the bounded select above with the same bounds breached on a number field.

```
╭──────────────────────────────────────────────────────────────────────────╮
│ Orchard order › Weight                                                   │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│ ❯ Basket weight (g)  1⑯█⑰                                                │
│                      Enter a number between 200 and 9000. ⑲              │
│     Weighed at the packing bench. ⑤                                      │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│ ↑/↓ adjust · ↵ accept · esc cancel ⑧                                     │
╰──────────────────────────────────────────────────────────────────────────╯
```

⑲ **error** - why the value was rejected. Replaces the constraint ⑬.

## What the code calls these today

| Part | In the source today |
|---|---|
| ① breadcrumb | `ThemeInterface::breadcrumb()` |
| ② marker | `ThemeInterface::marker()`, shared with ⑪ |
| ③ label | `ThemeInterface::label()`, `Field::$label` |
| ④ value | `ThemeInterface::value()` |
| ⑤ description | `ThemeInterface::description()`, shared with ⑬ and ⑭ |
| ⑥ instruction | `Field::$hint`, `DefaultTheme::renderFieldHint()`, `ThemeInterface::hint()` |
| ⑦ overflow | `ThemeInterface::indicator()`, `indicatorUp()`, `indicatorDown()` |
| ⑧ key legend | `WidgetInterface::hints()`, `Hint`, `ThemeInterface::keysHint()` |
| ⑨ selector | `ThemeInterface::check()`, `radio()` - no shared name |
| ⑩ entry | unnamed |
| ⑪ entry marker | `ThemeInterface::marker()`, shared with ② |
| ⑫ entry note | `Option::$disabledReason`, concatenated in `OptionsCapableTrait` |
| ⑬ constraint | `SelectionBoundedTrait::selectionHint()`, `FilePickerWidget::constraintHint()`, `NumberBounds::describe()` |
| ⑭ entry detail | `Option::$description` through `ThemeInterface::description()` |
| ⑮ location | unnamed |
| ⑯ buffer | `$buffer`, per widget - not in any shared API |
| ⑰ caret | `ThemeInterface::caret()` |
| ⑱ activity | `ThemeInterface::footer()`, used away from the footer |
| ⑲ error | `ThemeInterface::error()`, `WidgetInterface::error()` |

## Where the names disagree

**`hint` names four unrelated things.** `ThemeInterface::hint()` is a text style. `Field::$hint` is the instruction ⑥. `WidgetInterface::hints()` returns the key legend ⑧. `SelectionBoundedTrait::selectionHint()` returns the constraint ⑬. Nothing connects them, and the last one does not even use the `hint()` style - it draws through `description()`.

**`description` names three parts.** The field's description ⑤, the highlighted entry's detail ⑭ and the constraint ⑬ all render through `ThemeInterface::description()`. Restyling one restyles all three.

**The constraint has two names.** `SelectionBoundedTrait::selectionHint()` and `FilePickerWidget::constraintHint()` are the same part in two widgets, with the same body and two different names.

**`marker` covers two levels.** The row marker ② and the entry marker ⑪ are both `ThemeInterface::marker()`, so a theme cannot restyle one without the other.

**`error` names four things.** `ThemeInterface::error()` is a style, `WidgetInterface::error()` is widget state, `Field::templateError()` and friends produce a message, and `Engine::optionsError()` builds an exception.

**`footer` is drawn outside the footer.** The template widget's activity line ⑱ uses `ThemeInterface::footer()`, which otherwise styles the key legend ⑧ at the bottom of the frame.

**Three parts have no name at all.** The entry ⑩, the selector ⑨ and the location ⑮ are drawn inline with no named method behind them.
