---
title: Anatomy
description: 'The parts of a widget, named: the panel row, the open editor, and every line each of them draws.'
keywords: ['anatomy', 'nomenclature', 'parts', 'widgets', 'tui']
---

# Anatomy of a widget

Every field draws itself twice. In the panel it is a **row**: one line carrying the answer, with any supporting lines beneath it. Open the row and it becomes an **editor**: the widget takes over the space to the right of the label and draws whatever it needs to collect the answer.

This page names each part. The names are the vocabulary the rest of the documentation and the source use, so a bug report can say "the constraint line is wrong" and mean exactly one thing.

Every diagram below is real captured output, rendered in ASCII with color off.

## The row

A field that is not open draws one value line plus whatever supporting lines it declares.

```
+--------------------------------------------------------------------------+
| Anatomy > Basket                                                         |  (1) breadcrumb
+--------------------------------------------------------------------------+
|                                                                          |
| > Basket  apple                                                          |  (2) marker  (3) label  (4) value
|     Pick the produce for this delivery.                                  |  (5) description
|     Space toggles, Enter confirms.                                       |  (6) instruction
|                                                                          |
|   v                                                                      |  (7) overflow
|                                                                          |
+--------------------------------------------------------------------------+
| ^/v move * < select * esc back * q quit * ? help                         |  (8) key legend
+--------------------------------------------------------------------------+
```

## The editor

Opening the row hands the region right of the label to the widget. A multiple-choice field with bounds and option detail draws the fullest set of parts.

```
+--------------------------------------------------------------------------+
| Anatomy > Basket                                                         |  (1) breadcrumb
+--------------------------------------------------------------------------+
|                                                                          |
| > Basket    [x] Apple                                                    |  (9) selector  (10) entry
|           > [x] Carrot                                                   |  (11) entry marker
|             [ ] Tomato (out of season)                                   |  (12) entry note
|           Select between 2 and 3 items.                                  |  (13) constraint
|           Stays crisp for weeks when kept cold.                          |  (14) entry detail
|     Pick the produce for this delivery.                                  |  (5) description
|   v                                                                      |  (7) overflow
|                                                                          |
+--------------------------------------------------------------------------+
| ^/v move * < accept * esc cancel                                         |  (8) key legend
+--------------------------------------------------------------------------+
```

The file picker draws the same skeleton over a directory listing, and adds the one part unique to it.

```
+--------------------------------------------------------------------------+
| Anatomy > Price list                                                     |  (1) breadcrumb
+--------------------------------------------------------------------------+
|                                                                          |
| > Price list  sample-project                                             |  (15) location
|                 baskets/                                                 |  (10) entry
|               > deliveries/                                              |  (11) entry marker
|                 harvest.csv                                              |
|               Files only. Extensions: csv. Max 2 MB.                     |  (13) constraint
|     The CSV the orchard sends each week.                                 |  (5) description
|                                                                          |
+--------------------------------------------------------------------------+
| ^/v move * > open * < up * < select * tab hidden * esc cancel            |  (8) key legend
+--------------------------------------------------------------------------+
```

A text-shaped editor has no list at all, so it draws a buffer and a caret instead of entries.

```
+--------------------------------------------------------------------------+
| Anatomy > Crate                                                          |  (1) breadcrumb
+--------------------------------------------------------------------------+
|                                                                          |
| > Crate label  valley-pear|-a                                            |  (16) buffer  (17) caret
|                filling in Fruit                                          |  (18) status
|     Identifies the crate on the loading dock.                            |  (5) description
|                                                                          |
+--------------------------------------------------------------------------+
| v/^ next/previous * < accept * esc cancel                                |  (8) key legend
+--------------------------------------------------------------------------+
```

## One line, two states

The constraint (13) and the error (19) are the same physical line. The constraint is guidance shown *before* anything goes wrong; the moment a value is rejected the error replaces it, so the two never stack. Compare the bounded select above with the same bounds breached on a number field.

```
+--------------------------------------------------------------------------+
| Anatomy > Weight                                                         |  (1) breadcrumb
+--------------------------------------------------------------------------+
|                                                                          |
| > Basket weight (g)  1|                                                  |  (16) buffer  (17) caret
|                      Enter a number between 200 and 9000.                |  (19) error
|     Weighed at the packing bench.                                        |  (5) description
|                                                                          |
+--------------------------------------------------------------------------+
| ^/v adjust * < accept * esc cancel                                       |  (8) key legend
+--------------------------------------------------------------------------+
```

## Vocabulary

| # | Part | What it is |
|---|---|---|
| 1 | breadcrumb | The trail of panel titles above the content. |
| 2 | marker | The glyph showing which row has focus. |
| 3 | label | The field's name. |
| 4 | value | The settled answer, shown on a closed row. |
| 5 | description | The field's explanatory text. |
| 6 | instruction | The field's short how-to line, below the description. |
| 7 | overflow | The glyph showing content continues above or below. |
| 8 | key legend | The keys available right now, in the footer. |
| 9 | selector | The per-entry chosen-state glyph. |
| 10 | entry | One line of a list the widget offers. |
| 11 | entry marker | The glyph showing which entry is highlighted. |
| 12 | entry note | A qualifier on an entry, such as why it is unavailable. |
| 13 | constraint | What the field expects, shown before anything is rejected. |
| 14 | entry detail | The highlighted entry's own explanatory text. |
| 15 | location | The file picker's current directory. |
| 16 | buffer | The in-progress text, before it is accepted. |
| 17 | caret | The insertion point within the buffer. |
| 18 | status | What the widget is doing right now. |
| 19 | error | Why the value was rejected. Replaces the constraint (13). |

## Where the names disagree today

The table above is the target vocabulary. The source does not use it consistently yet, and the collisions below are why this page exists.

**`hint` names four unrelated things.** `ThemeInterface::hint()` is a text style. `$field->hint` is the instruction (6). `WidgetInterface::hints()` returns the key legend (8). `SelectionBoundedTrait::selectionHint()` returns the constraint (13). Nothing connects them, and the last one does not even use the `hint()` style - it draws through `description()`.

**`description` names three parts.** The field's description (5), the highlighted entry's detail (14) and the constraint (13) all render through `ThemeInterface::description()`. Restyling one restyles all three.

**The constraint has two names.** `SelectionBoundedTrait::selectionHint()` and `FilePickerWidget::constraintHint()` are the same part in two widgets, with the same body and two different names.

**`marker` covers two levels.** The row marker (2) and the entry marker (11) are both `ThemeInterface::marker()`, so a theme cannot restyle one without the other.

**`error` names four things.** `ThemeInterface::error()` is a style, `WidgetInterface::error()` is widget state, `Field::templateError()` and friends produce a message, and `Engine::optionsError()` builds an exception.

**`footer` is drawn outside the footer.** The template widget's status line (18) uses `ThemeInterface::footer()`, which otherwise styles the key legend (8) at the bottom of the frame.

**Three parts have no name at all.** The entry (10), the selector (9) and the location (15) are drawn inline with no named method behind them.
